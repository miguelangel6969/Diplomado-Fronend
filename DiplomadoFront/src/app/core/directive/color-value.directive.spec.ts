import { ColorValueDirective } from './color-value.directive';

describe('ColorValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorValueDirective();
    expect(directive).toBeTruthy();
  });
});
